"Please read the attached AI_CONTEXT.md file (v1.3). We are building the Category page (src/pages/Categories.tsx) for the hooknhunt-ui project.

Tech Stack (must use): React, TypeScript, Shadcn, TailwindCSS, Zustand, react-router-dom.

Task: Update the src/pages/Categories.tsx file.

Requirements:

Data Fetching:

Use react-query or a simple useEffect + useState hook to fetch data.

On page load, call the API endpoint GET /api/v1/admin/categories (defined in AI_CONTEXT.md, Section 9.2).

Create an apiClient.ts helper file (using axios) that automatically includes the token from the authStore in the headers (Authorization: Bearer <token>).

State:

Maintain states for categories: Category[], isLoading: boolean, and error: string | null.

UI (Loading State):

If isLoading is true, display a Shadcn Skeleton loader (e.g., 3 rows of skeleton table).

UI (Data State):

Use Shadcn Table (Table, TableHeader, TableHead, TableRow, TableCell) to display the list of categories.

Show columns for: Name, Slug, and Parent (parent name, if it exists).

Add a "Add New" button (using Shadcn Button) at the top right of the page.

UI (Empty State):

If isLoading is false and categories.length === 0, show a message like "No categories found. Create one!"

Provide the code for:

src/lib/apiClient.ts (The reusable axios instance)

src/pages/Categories.tsx (The main page component with data fetching, skeleton, and table) "