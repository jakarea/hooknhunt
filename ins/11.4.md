You are working in `hooknhunt-ui`. We need to build the **Purchase Order Details** page with a workflow timeline and status management.

**Task 1: Update `src/stores/purchaseStore.ts`**
Add these actions:
* `fetchOrder(id: number)`: Calls `GET /api/v1/admin/purchase-orders/{id}`.
* `updateStatus(id: number, status: string, data: any)`: Calls `PUT /api/v1/admin/purchase-orders/{id}/status`.
* `receiveItems(id: number, data: any)`: Calls `POST /api/v1/admin/purchase-orders/{id}/receive-items`.

**Task 2: Create `src/pages/purchase/PurchaseOrderDetails.tsx`**

**UI Components:**
1.  **Header:**
    * Order # (e.g., PO-202511-01).
    * Current Status Badge (Color coded).
    * **Timeline/Stepper:** Visual display of steps: Draft -> Payment -> Dispatched -> Shipped BD -> Transit Bogura -> Received -> Completed. Highlight the current step.

2.  **Main Action Button:**
    * Located at the top-right.
    * Text changes based on status (e.g., "Confirm Payment", "Dispatch", "Receive Stock").
    * **Clicking opens a Dialog (Modal):**
        * *Payment:* Input `Exchange Rate` (Default from settings).
        * *Dispatched:* Input `Courier Name`, `Tracking No`.
        * *Shipped BD:* Input `Lot Number`.
        * *Transit:* Input `BD Courier Tracking`.
        * *Received:* Opens the **Receive Stock Modal** (See below).

3.  **Receive Stock Modal (Complex):**
    * List each Ordered Item (Parent Product).
    * Allow splitting Parent Product into Variants:
        * Show "Ordered: 100 pcs".
        * Dynamic Rows: [ Select Variant (Dropdown) ] [ Input Qty ].
        * "Add Variant Row" button.
        * Input "Lost Qty".
        * Validation: Sum of (Variant Qtys + Lost Qty) MUST equal Ordered Qty.
    * Inputs for `Total Weight` and `Extra Cost`.
    * Submit Button: "Finalize & Add to Inventory".

4.  **Order Items Table:**
    * List products, original RMB price, Qty.
    * If status is `arrived_bd` or later, show input/display for `Shipping Cost` per item.

**Tech Stack:** React, Shadcn (Dialog, Input, Button, Table, Select), Lucide Icons.

Provide the code for:
1.  Updated `purchaseStore.ts`.
2.  `PurchaseOrderDetails.tsx`.