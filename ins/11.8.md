You are working in the `hooknhunt` monorepo. We need to upgrade the **Global Settings** module to handle Profit Margins, Payment Gateways, and Tracking IDs.

### PART 1: BACKEND (`hooknhunt-api`)

**Task: Update `database/seeders/SettingSeeder.php`**
Add these new default keys to the existing `settings` table seeder:

```php
// Pricing Defaults (Percentage)
['key' => 'default_margin_retail', 'value' => '50'],
['key' => 'default_margin_wholesale', 'value' => '20'],
['key' => 'default_margin_daraz', 'value' => '60'],

// Payment Gateway Config (Empty defaults)
['key' => 'sslcommerz_store_id', 'value' => ''],
['key' => 'sslcommerz_store_password', 'value' => ''],
['key' => 'sslcommerz_is_sandbox', 'value' => '1'], // 1=True
['key' => 'bkash_app_key', 'value' => ''],
['key' => 'bkash_app_secret', 'value' => ''],

// Tracking / Marketing
['key' => 'facebook_pixel_id', 'value' => ''],
['key' => 'google_analytics_id', 'value' => ''],
Note: Do not create a migration, just update the seeder so we can run db:seed.

PART 2: FRONTEND (hooknhunt-ui)
Task: Update src/pages/settings/GlobalSettings.tsx Refactor the existing page to use Shadcn Tabs.

UI Structure:

Header: "Global Settings".

Tabs: [General], [Pricing], [Payment], [Tracking].

Tab Content:

General:

Input: Exchange Rate (Existing).

Pricing (Profit Margins):

Card Title: "Default Profit Margins (%)".

Inputs: Retail (50%), Wholesale (20%), Daraz (60%).

Note: These will be used to auto-calculate prices during stock entry.

Payment Integrations:

SSLCOMMERZ Section: Inputs for Store ID, Password, and a Checkbox/Switch for "Sandbox Mode".

bKash/Nagad Section: Inputs for App Keys (Placeholders).

Tracking & Analytics:

Input: Facebook Pixel ID.

Input: Google Analytics Measurement ID.

Logic:

Use useSettingStore to fetch all settings on mount.

Use react-hook-form to handle the large form.

"Save Changes" button should submit all data to POST /api/v1/admin/settings.

Provide code for:

SettingSeeder.php (Updated array).

GlobalSettings.tsx (Complete file with Tabs).