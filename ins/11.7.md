You are working in the `hooknhunt` monorepo. Read `AI_CONTEXT.md`.

**Goal:** Create the **Inventory List** page to view current stock levels.

---

### PART 1: BACKEND (`hooknhunt-api`)

**Task:** Update `app/Http/Controllers/Api/V1/Admin/InventoryController.php`.

1.  **Add `index` method:**
    * Fetch all records from the `inventory` table.
    * **Eager Load:** `productVariant.product` (Parent), `productVariant` (SKU, Prices).
    * **Pagination:** 20 items per page.
    * **Filter (Optional but good):** Allow filtering by `product_id` or `sku`.
    * **Response:** JSON with pagination meta.

2.  **Update Route:**
    * Ensure `GET /inventory` points to `[InventoryController::class, 'index']`.

---

### PART 2: FRONTEND (`hooknhunt-ui`)

**Task 1: Update `src/stores/inventoryStore.ts`** (Create if missing)
* State: `inventoryItems: InventoryItem[]`, `isLoading`.
* Action: `fetchInventory(page: number, filters?: any)`.

**Task 2: Create `src/pages/inventory/InventoryList.tsx`**
* **Header:** "Current Stock".
* **Table Columns:**
    * **Image/Product:** Thumbnail + Parent Name.
    * **Variant:** SKU + Attributes (Color/Size).
    * **Landed Cost:** Show cost price (Permission Check: Admin only).
    * **Sale Price:** Retail Price.
    * **Stock Qty:** The `quantity` field (Highlight Low Stock in Red).
    * **Stock Value:** `Qty * Landed Cost` (Admin only).
* **Pagination:** Add pagination controls at the bottom.

**Task 3: Integration**
* Add route: `/inventory` -> `InventoryList`.
* Add to Sidebar Menu.

Provide the code for:
1.  `InventoryController.php` (index method).
2.  `inventoryStore.ts`.
3.  `InventoryList.tsx`.