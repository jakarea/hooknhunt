Please read the attached AI_CONTEXT.md file (v1.3). We will now create a reusable form component for adding and editing suppliers.

Task: Create a new file: src/components/forms/SupplierForm.tsx.

Requirements:

Tech Stack: Must use react-hook-form, zod, and @hookform/resolvers/zod.

Store: Must import SupplierFormData, addSupplier, and updateSupplier from src/stores/supplierStore.ts.

Props: The component must accept initialData?: Supplier | null and onClose: () => void.

Validation: Create a zod schema named formSchema that validates the SupplierFormData (e.g., name (required), shop_name (optional), email (optional, must be valid email), shop_url (optional, must be valid URL)).

Form UI:

Use Shadcn components (Form, FormField, FormItem, FormLabel, FormControl, FormMessage, Input).

Create input fields for: name (Contact Person), shop_name, email, shop_url, wechat_id, alipay_id, contact_info (use Textarea).

Submit Logic:

The onSubmit function must check if initialData exists.

If initialData exists: Call updateSupplier(initialData.id, values).

If initialData is null: Call addSupplier(values).

Show Shadcn toast on success or error.

Call onClose() on successful submission.

The submit button must show a loading state (isLoading).

Provide only the code for src/components/forms/SupplierForm.tsx