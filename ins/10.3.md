Please read the attached AI_CONTEXT.md file (v1.3). We need to implement the UI to attach suppliers to a product.

Task 1: Update src/stores/productStore.ts

Add the attachSupplier and detachSupplier actions to the store.

TypeScript

// Add inside ProductState interface
attachSupplier: (productId: number, supplierId: number, urls: string[]) => Promise<void>;
detachSupplier: (productId: number, supplierId: number) => Promise<void>;

// Add inside create store implementation
attachSupplier: async (productId, supplierId, urls) => {
  await apiClient.post(`/admin/products/${productId}/suppliers`, {
    supplier_id: supplierId,
    supplier_product_urls: urls
  });
  // Note: ideally, we should re-fetch the single product to update the UI
  await get().fetchProduct(productId); 
},

detachSupplier: async (productId, supplierId) => {
  await apiClient.delete(`/admin/products/${productId}/suppliers/${supplierId}`);
  await get().fetchProduct(productId);
},
Task 2: Create src/components/forms/product/ProductSuppliersTab.tsx

Create a component that allows users to manage suppliers for a specific product.

Requirements:

Props: product: Product (The full product object, which includes suppliers).

Hooks: Use useSupplierStore (to list all available suppliers in dropdown) and useProductStore (to call attach/detach actions).

Effect: On mount, call fetchSuppliers() from useSupplierStore so the dropdown is populated.

Local State:

selectedSupplierId: string

urls: string[] (Start with [''])

UI Layout:

Card 1: Add Supplier

Select Dropdown: 'Select a Supplier' (map over supplierStore.suppliers).

Dynamic URL Inputs: Map over urls. Add 'Plus' button to add new line, 'Trash' to remove line.

Button: 'Attach Supplier'. On click -> validate -> call attachSupplier -> reset form.

Card 2: Attached Suppliers

Table displaying: Supplier Name, Links (show as clickable badges or list), Action (Detach Button).

Task 3: Integration Instruction

Show me how to import and use this ProductSuppliersTab inside the existing ProductEdit.tsx (or ProductForm.tsx) using Shadcn Tabs.

Provide the code for ProductSuppliersTab.tsx.