Please read the attached AI_CONTEXT.md file (v1.3). We will now create the Supplier List page (src/pages/purchase/Suppliers.tsx).

Task: Create the src/pages/purchase/Suppliers.tsx file.

Requirements:

Store: Must use the useSupplierStore (created in Step 9.1).

Data Fetching: Call the fetchSuppliers() action on component mount (useEffect).

State: Get suppliers, isLoading from the store.

Role Check: Get the user from useAuthStore to check their role.

Loading UI: If isLoading is true, show 3 rows of Shadcn Skeleton (e.g., <Skeleton className="h-10 w-full" />).

Table UI:

Use Shadcn Table (Table, TableHeader, TableHead, TableRow, TableCell).

Show columns for: Shop Name, Contact Person, Email, Phone (from contact_info or phone if added).

Header:

Show "Suppliers" as the title.

Show an "Add New Supplier" button (Shadcn Button).

Wrap this button in a <RoleGuard /> component. The button must only be visible for allowedRoles={['super_admin', 'admin', 'store_keeper']}.

Actions Column:

Add an "Actions" column to the table.

This entire column (<TableHead> and <TableCell>) must also be wrapped in a <RoleGuard /> component with allowedRoles={['super_admin', 'admin', 'store_keeper']}.

(We will add the Edit/Delete buttons to this cell in the next step).

Provide only the code for src/pages/purchase/Suppliers.tsx.