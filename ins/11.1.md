Please read the attached `AI_CONTEXT.md` file. We need to implement a Global Settings system to store configuration values like Exchange Rates.

**Task 1: Create Migration**
Create a migration file: `create_settings_table`.
- Columns:
    - `id` (PK)
    - `key` (string, unique) - e.g., 'exchange_rate_rmb_bdt'
    - `value` (text, nullable)
    - `created_at`, `updated_at`

**Task 2: Create Seeder**
Create a seeder: `SettingSeeder`.
- Insert a default row:
    - `key`: 'exchange_rate_rmb_bdt'
    - `value`: '17.50' (Default rate)

**Task 3: Create Controller**
Create `app/Http/Controllers/Api/V1/Admin/SettingController.php`.
- **`index()`**: Return all settings as a key-value pair object (e.g., `{'exchange_rate_rmb_bdt': '17.50'}`).
    - Hint: `Setting::pluck('value', 'key')`.
- **`update(Request $request)`**:
    - Accept an array of settings.
    - Loop through the request data and update/create keys in the database.
    - Example Body: `{'exchange_rate_rmb_bdt': '18.00'}`.
    - Return the updated settings.

**Task 4: Update Routes**
Update `routes/api.php` inside the `v1/admin` -> `auth:sanctum` -> `role:super_admin` group (Settings should be restricted to Super Admin only).
- `GET /settings`, [SettingController::class, 'index']
- `POST /settings`, [SettingController::class, 'update']

Provide the code for:
1. Migration file
2. SettingSeeder
3. SettingController
4. Routes update